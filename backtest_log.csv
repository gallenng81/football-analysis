name: Track market odds

on:
  schedule:
    # Runs at 06:00 and 18:00 UTC daily. football-data.co.uk itself only
    # refreshes fixture odds Fri/Tue afternoons UK time, so most runs will
    # just re-log the same odds until the next real market update - that's
    # fine, it just means no movement to report between those refreshes.
    - cron: "0 6,18 * * *"
  workflow_dispatch: {}  # lets you trigger a run manually from the Actions tab

permissions:
  contents: write  # needed so the workflow can commit the updated history file back

jobs:
  track-odds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pandas requests

      - name: Fetch and log current odds
        run: python fetch_market_odds.py --leagues E0 SP1 D1 I1 F1

      - name: Commit updated history
        run: |
          git config user.name "odds-tracker-bot"
          git config user.email "actions@github.com"
          git add data/market_odds_history.csv
          git diff --staged --quiet || git commit -m "Log market odds snapshot"
          git push
